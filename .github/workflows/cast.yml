name: Cast to Farcaster

on:
    release:
        types: [published]

jobs:
    output-information:
        runs-on: ubuntu-latest
        steps:
            - name: Output Information
              run: echo ${{ github.event.action }}
